<section class="inner">
  <h2 class="section-ttl">環境タグ編集</h2>
  <% if admin_signed_in? %>
    <%= render 'form', environment: @environment, url: admin_environment_path(@environment), method: :PATCH, submit: '更新' %>
  <% end %>
</section>