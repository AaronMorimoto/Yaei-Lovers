<div class='container px-5 px-sm-0  form-box'>
  <div class="row">
    <div class="offset-1 text-center">
      <h2 class="text-center px-3 py-3">
        <strong>県名タグ編集</strong>
      </h2>
    </div>
  </div>
  <div class='row'>
    <div class='col'>
      <div class="mb-3">
        <section class="inner">
          <% if admin_signed_in? %>
            <%= render 'form', prefecture: @prefecture, url: admin_prefecture_path(@prefecture), method: :PATCH, submit: '更新' %>
          <% end %>
        </section>
      </div>
    </div>
  </div>
</div>